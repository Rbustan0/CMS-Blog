{{!-- SAMPLE PAGE FOR SINGULAR POST --}}

<div class="text-center">
  <h2>{{post.title}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{post.content}}</p>
  <p>Created by {{user.username}} on {{format_date post.date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div>
